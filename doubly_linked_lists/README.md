# 0x17. C - Doubly linked lists

This project implements doubly linked lists in C using the `dlistint_t` structure, where each node points to both the previous and the next node. It includes functions to print, count, add nodes at the start or end, free the list, get a node by index, sum values, and insert or delete a node at a given index.
