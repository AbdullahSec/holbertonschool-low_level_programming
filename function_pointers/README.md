#This project explores function pointers in C: what they hold and where they point to in virtual memory. It includes functions that pass, iterate, and select behavior through function pointers, plus a simple command-line calculator.
EOF 0x0F. C - Function pointers

This project explores function pointers in C: what they hold and where they point to in virtual memory. It includes functions that pass, iterate, and select behavior through function pointers, plus a simple command-line calculator.
