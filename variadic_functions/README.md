# 0x0F. C - Variadic functions

This project covers variadic functions in C: functions that accept a variable number of arguments using the `va_start`, `va_arg`, and `va_end` macros from `stdarg.h`. It includes functions that sum, print numbers, print strings, and print mixed types based on a format string.
