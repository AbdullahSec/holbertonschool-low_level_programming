# 0x1A. C - Hash tables

This project implements a hash table in C with chaining for collision handling. It includes functions to create a hash table, hash keys with the djb2 algorithm, compute a key's index, add or update key/value pairs, retrieve values, print the table, and free it.
