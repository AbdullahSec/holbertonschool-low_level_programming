# 0x15. C - File I/O

This project covers low-level file operations in C using POSIX system calls (open, read, write, close). It includes functions to read and print a text file, create a file with specific permissions, append text to a file, and a program that copies one file's content to another.
