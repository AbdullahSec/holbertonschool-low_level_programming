# 0x12. C - Singly linked lists

This project introduces singly linked lists in C using the `list_t` structure. It includes functions to print a list, count its nodes, add a node at the beginning or the end, and free the whole list.
