# 0x0D. C - argc, argv

This project covers how a C program receives command-line arguments through `argc` and `argv`, and how to handle unused parameters with `(void)`. It includes programs that print the program name, count arguments, print all arguments, multiply two numbers, and add a list of positive numbers.
