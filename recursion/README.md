# 0x08. C - Recursion

This project implements classic algorithms using recursion in C: printing a string forward and in reverse, string length, factorial, power, natural square root, and prime number checking. Each function calls itself with a smaller sub-problem until it reaches a base case.
